// Re-export the refactored StaffAdvisorDashboard component
export { StaffAdvisorDashboard } from "./StaffAdvisorDashboard/index";

// Re-export types for backward compatibility
export type { Student, FacultyStatus, DashboardStats, CareerStats } from "./StaffAdvisorDashboard/types";
